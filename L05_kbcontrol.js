// write your codes here
let objsize, objX, objY, objSpeed;




function setup(){
    createCanvas(600,300);
    background(220)
}
function draw(){
    if(keycode === 37){
        objX -= objSpeed
    }
    if(leycode === 39){
        
    }
}